stages:
  - test
variables:
  AUTONOMA_CLIENT_ID: $AUTONOMA_CLIENT_ID
  AUTONOMA_CLIENT_SECRET: $AUTONOMA_CLIENT_SECRET
run-folder-cmbjpl99c0001p4yg40qfygel:
  name: "test-folder"
  stage: test
  image: axlscld/autonoma-runner:1.0.0
  variables:
    ITEM_ID: "cmbjpl99c0001p4yg40qfygel"
    ITEM_TYPE: "folder"
    MAX_WAIT_TIME: "10"
  script:
    - |
      /autonoma/gitlab-run-test.sh \
        "$ITEM_ID" \
        "$MAX_WAIT_TIME" \
        "$AUTONOMA_CLIENT_ID" \
        "$AUTONOMA_CLIENT_SECRET" \
        "$ITEM_TYPE"
  when: always
run-test-cmbf7f3ko0003p4y8r7spvguf:
  name: "Search and subscribe ThePrimeagen"
  stage: test
  image: axlscld/autonoma-runner:1.0.0
  variables:
    ITEM_ID: "cmbf7f3ko0003p4y8r7spvguf"
    ITEM_TYPE: "test"
    MAX_WAIT_TIME: "10"
  script:
    - |
      /autonoma/gitlab-run-test.sh \
        "$ITEM_ID" \
        "$MAX_WAIT_TIME" \
        "$AUTONOMA_CLIENT_ID" \
        "$AUTONOMA_CLIENT_SECRET" \
        "$ITEM_TYPE"
  when: always
