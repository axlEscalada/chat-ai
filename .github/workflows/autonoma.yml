name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  run-test-cmddlmcte000tw0015nbmv6hd:
    runs-on: ubuntu-latest
    name: Autonoma Test "Github action healthcheck autonoma test"
    steps:
      - name: Run Single Test "Github action healthcheck autonoma test"
        id: step-1
        uses: autonoma-ai/actions/test-runner@v1
        with:
          test-id: 'cmddlmcte000tw0015nbmv6hd'
          client-id: ${{ secrets.AUTONOMA_CLIENT_ID }}
          client-secret: ${{ secrets.AUTONOMA_CLIENT_SECRET }}
          max-wait-time: '10' #IN MINUTES
      - name: Show "Github action healthcheck autonoma test" results
        if: always()
        run: |
          echo "Test status: ${{ steps.step-1.outputs.final-status }}"
          echo "Message: ${{ steps.step-1.outputs.message }}"
          echo "View results at: ${{ steps.step-1.outputs.url }}"
